apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller
  namespace: projectsveltos
spec:
  template:
    spec:
      containers:
      # Change the value of image field below to your controller image URL
      - image: docker.io/projectsveltos/k8s-cleaner:v0.12.0
        name: controller
